{
  "name": "vitest-sample",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.1.1",
    "@vitest/coverage-v8": "^3.1.1"
  }
}